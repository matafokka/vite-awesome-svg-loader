# vite-awesome-svg-loader

This package bundles all other packages together. In other words, it acts like a meta-package.
`dist` directory is the final library bundle.

Entries, configs, and exports inside `package.json` are auto-generated.
