# Tests for vite-awesome-svg-loader/vanilla-integration package

This test suite uses Cypress framework and follows its conventions.

## Technical requirements

1. Because plain JS classes are being tested, assertions must run synchronously.
1. Use Chai assertions.
1. Avoid Cypress assertions.
